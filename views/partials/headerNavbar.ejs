<header>
  <nav class="navbar navbar-expand-lg">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">Inicio</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link inactive" data-toggle="dropdown" href="#" role="button" aria-expanded="false">Leyes</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="/leyes/search">Buscar Ley</a>
            <% if (locals.isLogged) { %>
              <% if (locals.loggedUser.admin == 1) { %>
                <a class="dropdown-item" href="/leyes/create">Cargar Ley</a>
              <% } %>
            <% } %> 
          </div>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link" data-toggle="dropdown" href="#" role="button" aria-expanded="false">Doctrinas</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="/doctrinas/search">Buscar Doctrina</a>
            <% if (locals.isLogged) { %>
              <% if (locals.loggedUser.admin == 1) { %>
                <a class="dropdown-item" href="/doctrinas/create">Cargar Doctrina</a>
              <% } %>
            <% } %>  
          </div>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link" data-toggle="dropdown" href="#" role="button" aria-expanded="false">Escritos</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="/escritos/search">Buscar Escrito</a>
            <% if (locals.isLogged) { %>
              <% if (locals.loggedUser.admin == 1) { %>
              <a class="dropdown-item" href="/escritos/create">Cargar Escrito</a>
              <% } %>
            <% } %>  
          </div>
        </li>
        <!--<li class="nav-item">
          <a class="nav-link" href="/leyes/list">Explorar</a>
        </li> -->


        <% if (locals.isLogged) { %>
          <li class="nav-item dropdown">
            <a class="nav-link" data-toggle="dropdown" href="#" id="dropUser" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <img src="/images/avatars/<%= locals.loggedUser.avatar %>" width="40" style="border-radius: 100%;">
              Hi <%= locals.loggedUser.firstName %> 
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <a class="dropdown-item" href="/users/profile">Perfil de Usuario</a>
              <li><hr class="dropdown-divider"></li>
              <a class="dropdown-item" href="/users/logout">Cerrar Sesión</a>
              <% if (locals.loggedUser.admin == 1) { %>
                <li><hr class="dropdown-divider"></li>
                <a class="dropdown-item" href="#">
                    Configurar
                </a>
              <% } %>  
            <!-- </ul> -->
          </li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/users/login">Login</a></li>
        <% } %>
        </ul>
    </div>
  </nav>
</header>